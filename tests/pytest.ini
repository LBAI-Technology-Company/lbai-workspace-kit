[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: pure function tests, no subprocess
    integration: tool subprocess tests in isolated git workspace
    e2e: multi-command pipeline tests
    quality: prompt/schema/contract consistency checks
